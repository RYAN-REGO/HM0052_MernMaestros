'use client';
'use strict';

var React = require('react');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var React__default = /*#__PURE__*/_interopDefault(React);

function isGroup(input) {
  return "group" in input;
}
function NativeSelectOption({ data }) {
  if (isGroup(data)) {
    const items = data.items.map((item) => /* @__PURE__ */ React__default.default.createElement(NativeSelectOption, { key: item.value, data: item }));
    return /* @__PURE__ */ React__default.default.createElement("optgroup", { label: data.group }, items);
  }
  const { value, label, ...others } = data;
  return /* @__PURE__ */ React__default.default.createElement("option", { key: data.value, value: data.value, ...others }, data.label);
}
NativeSelectOption.displayName = "@mantine/core/NativeSelectOption";

exports.NativeSelectOption = NativeSelectOption;
//# sourceMappingURL=NativeSelectOption.cjs.map
